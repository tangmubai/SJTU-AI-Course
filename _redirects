# /practice is served from practice.html automatically by Cloudflare's
# clean-URL handling (html_handling); no rewrite rules needed here.
# Rewriting to a .html path here would trigger a redirect loop.
